import java.io.*;

public class mainClass {

    public static void main(String[] args) throws IOException {
            LearningModel lm = new LearningModel();
            // lm.train(); to Train the Model
            // lm.predict(filename);to Predict new data
    }

}
